<div>
    <h1>¡Hola, {{user.name}} {{user.last_name}}! ¡Que gusto verte!</h1>
</div>
<div>
    <p>
        tu edad es de : {{user.edad}}
    </p><br>
    <p>
        tu correo es {{user.correo}}</p><br>
    <p>
        tu telefono es {{user.telefono}}
    </p>
    {{#if isAdmin}}
        {{#each food}}
            <div>
                <p>{{this.name}}</p>
                <p>{{this.price}}</p>
            </div>
            {{/each}}
        {{else}}
            <p>No tienes permisos para ver la lista de alimentos</p>
    {{/if}}
</div>